# Copy to .env and fill in. The anon key is safe to expose — RLS protects the data.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=

# Real domain, used for canonical URLs + sitemap at build time.
SITE_URL=https://meenakshidairyfarms.example
