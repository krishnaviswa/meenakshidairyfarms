---
import OrderPage from "../../components/pages/OrderPage.astro";
import type { Lang } from "../../i18n";
export function getStaticPaths() {
  return [{ params: { lang: "hi" } }, { params: { lang: "ta" } }];
}
const lang = Astro.params.lang as Lang;
---
<OrderPage lang={lang} />
